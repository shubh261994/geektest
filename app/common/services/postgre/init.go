package postgre

import (
	"geektest/config"
)

const host = config.PostgreHost
const port = config.PostgrePort
const user = config.PostgreUser
const pass = config.PostgrePass
const dbName = config.PostgreDbName
